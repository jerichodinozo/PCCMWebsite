import ModalImage from "react-modal-image";

function MyModalImage() {
    return(
        <ModalImage className="modal-image"
                        small={item}
                        large={item}
        />
    );
}